extends layout

block content
    .ui.container
        .ui.basic.center.aligned.segment
            h1 MovieNews
            p(style="display: inline-block; margin-top:150px;") Show me news for &nbsp;&nbsp;&nbsp;
            .ui.icon.input
                input(type='text' placeholder='Search...' style="display: inline-block; margin-top:150px;")
                i.search.icon(style="margin-top:75px;")

